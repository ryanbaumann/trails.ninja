/** Canonical factor labels: the same factor names everywhere. */
export const FACTOR_LABELS: Record<string, string> = {
  visibility: 'Visibility',
  condition: 'Budget fit',
  activity: 'Vibe',
  access: 'Access',
  environment: 'Environment',
} as const;
