import { OutputLayout } from '../types';

export const OUTPUT_LAYOUT_LABELS: Record<OutputLayout, string> = {
  single: 'Single Reveal',
  'salon-sheet': 'Salon Sheet',
  'before-after': 'Before / After',
};
