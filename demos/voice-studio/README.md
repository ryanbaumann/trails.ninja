# Voice & Editorial Studio

Interactive blind review, grading, and paired dataset curator for local Gemma 4 voice fine-tuning.

`npm install && npm run dev`

The browser key must be HTTP-referrer and API restricted. Secret-bearing calls belong in a bounded gateway handler.
