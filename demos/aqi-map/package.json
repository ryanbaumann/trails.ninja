{
  "name": "aqi-map",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Live air quality heatmap on a 2D Google map, powered by the Google Maps Platform Air Quality API.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test test/*.test.js && npm run build"
  },
  "dependencies": {
    "@googlemaps/js-api-loader": "^2.1.1",
    "vite": "^8.1.5"
  },
  "engines": {
    "node": ">=20"
  }
}
